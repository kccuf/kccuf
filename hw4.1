#include <iostream>
#include <math.h>

using namespace std;

int main()
{
	int a, max=-1000, min=1000,b;
	cout<<"Vvedite a"<<endl;
	cin>>a;
	int c[a];
	cout<<"Vvedite elementi massiva"<<endl;
	for (int i=0;i<a;i++)
    {
        cin>>b;
        c[i]=b;
        if (b<min) min=b;
        if (b>max) max=b;
    }
    cout<<"min="<<min<<"   max="<<max<< endl;

	system("pause");
}
